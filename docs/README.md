# FamiRust hardware references

Clean-room only: work from hardware documentation, never from third-party
emulator source. Drop the reference material the user supplies here (2A03/6502
datasheets, the NESdev wiki notes, PPU/APU timing docs, mapper specs, the
TomHarte 6502 vector notes). Cite the doc + section in code comments when a
non-obvious timing or quirk is implemented.

Pending from the user: dumps, any BIOS-ish images (FDS disksys), and docs.
